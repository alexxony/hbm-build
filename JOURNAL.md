# JOURNAL — hbm_build 원장

> 규약: 상태 변화마다 1~2줄 즉시 append + 커밋. 시각은 ISO 8601+09:00(24h).
> 요약은 vault PROGRESS.md(마감 전용). 기계 판정은 epoch 초 산술만.

- 2026-07-17T22:17:10+09:00 — P4 T3 1차: A-S0 케이스 완주, `results/p4_icepak_scenarios/p4_icepak_a_s0.csv` 생성 (base_die avg 180.10 / max 193.68°C). 직후 ~22:20 오케스트레이터가 cp949 오전/오후 오독으로 solve 프로세스 오살 — 1차 중단의 실제 원인.
- 2026-07-17T22:31:32+09:00 — P4 T3 2차: AEDT 이중 기동(22:31:18 + 22:31:32) → 2번째 인스턴스 FlexNet -5 `icepak_gui` 라이선스 획득 실패로 배치 사망. 증거: `results/p4_icepak_scenarios/t3_attempt2_batch.log`. 교훈: Student 라이선스는 단일 인스턴스 — 동시 기동 금지.
- 2026-07-18T20:58:33+09:00 — P4 T3 3차 착수: 포렌식으로 위 2건 원인 확정, A-S0 CSV 유효 판정(ΔT비 1.8750 = 30/16 정확, H1a 선형 정합) → 회수 커밋. 잔여 5케이스(A-S1·A-S2·B-S0·B-S1·B-S2) executor 위임.
- 2026-07-18T21:57:43+09:00 — P4 T3 3차: A-S1 실패. 증거(`results/p4_icepak_scenarios/run_a_s1.log`, UTF-16): 지오메트리·재료·BC(3 sub-box 소스 10건+heatsink wall) 전부 정상 생성, `Project hbm2e_p4_a_s1 Saved correctly` 직후 `PyAEDT ERROR: Error in solving all setups (AnalyzeAll)`, `Design setup None solved correctly in 0.0h 0.0m 5.0s` — 5초 즉시 실패(발산 아님, setup 이름 미등록 의심). 직전 경고: `No mesh operation found` / `No mesh region found`, `Property Command is read-only`. AEDT 정상 해제, 프로세스 0 확인. 코드 경로는 A-S0과 동일(`create_conduction_setup()`/`build_and_solve()` 무분기) — power_scenario 값 차이만으로 결정론적 코드 버그를 특정하지 못함, 타이밍/레이스 의심(setup.update()와 analyze() 사이 gRPC 비동기 지연 가능성). 재시도 1회 진행(동일 커맨드, 신규 프로젝트명 유지).
- 2026-07-18T23:55:13+09:00 — P4 T3 3차: A-S1 재시도(retry1, 신규 프로젝트명 hbm2e_p4_a_s1_r2) 동일 지점 동일 실패(`Design setup None solved correctly in 0.0h 0.0m 5.0s`, 5초 즉시). 로그: `results/p4_icepak_scenarios/run_a_s1_retry1.log`(UTF-8). 케이스 재시도 한도(1회) 소진. **타이밍/레이스 가설 기각** — 2회 연속 동일 실패는 결정론적 버그. A-S0(시나리오 없음) 성공 vs A-S1(--power-scenario s1) 2연속 실패 = power_scenario 조합 의존 확정. 캠페인 중단 유지, 코드 진단 국면 착수.
- 2026-07-18T23:58:41+09:00 — P4 T3 3차: 코드 진단 완료. `git diff 64e3720..HEAD -- scripts/build_icepak_model.py model_config.py` 확인 — model_config.py 무변경, build_icepak_model.py는 --bottom-htc 스레딩만 추가(A-S1은 bottom_htc_w_m2k=None이라 no-op). P3 T4(16W+s1) 성공 vs P4 A-S1(30W+s1) 2연속 실패 = 코드 분기가 아니라 **pyaedt 내부 예외 삼킴** 문제로 판정. 근거: `.venv/Lib/site-packages/ansys/aedt/core/application/analysis.py` L1809-1821 `analyze_setup()` — name=None(우리 analyze() 호출 경로)일 때 `self.odesign.AnalyzeAll(blocking)`을 try/except Exception으로 감싸 **원 네이티브 예외를 버리고** "Error in solving all setups (AnalyzeAll)." 제네릭 텍스트만 로깅(L1819-1821). "Design setup None solved correctly"도 L1851의 상시 출력(name 그대로 찍음, 실패와 무관) — setup 이름 미등록 의미 아님. create_setup(name=None)→generate_unique_setup_name()이 "Setup" 등 자동 이름을 정상 부여하므로(analysis.py L1145-1151) setup 자체는 정상 등록됨, 즉 "Design setup None"은 misleading 로그였다. 수정: `scripts/build_icepak_model.py` solve_at_mesh_resolution()에 analyze() 실패 시 `ipk.odesktop.GetMessages("", "", 0)` 네이티브 메시지 매니저 조회 + `ipk.setup_names` 조회를 추가해 pyaedt가 삼킨 실제 네이티브 오류를 회수하도록 진단 계측 삽입(커밋 예정). 다음: WSL 커밋 → Windows pull → A-S1 1회 검증 실행(run_a_s1_fix1.log).
